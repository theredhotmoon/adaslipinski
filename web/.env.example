# Laravel headless CMS API (server-side, read at build time in .astro frontmatter).
# No PUBLIC_ prefix → never shipped to the browser.
CMS_API_URL=http://localhost:8000/api

# Canonical public origin — used for <link rel="canonical">, hreflang, sitemap.xml.
PUBLIC_SITE_URL=https://adaslipinski.pl
